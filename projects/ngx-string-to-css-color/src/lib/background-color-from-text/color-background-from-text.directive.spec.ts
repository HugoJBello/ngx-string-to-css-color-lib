import { ColorBackgroundFromTextDirective } from './color-background-from-text.directive';

describe('ColorBackgroundFromTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBackgroundFromTextDirective();
    expect(directive).toBeTruthy();
  });
});
